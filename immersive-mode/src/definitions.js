/**
 * @typedef {Object} ImmersiveModePlugin
 * @property {() => Promise<void>} activate - Hide status + navigation bars (Android).
 * @property {() => Promise<void>} deactivate - Show system bars again (Android).
 */

export {};
